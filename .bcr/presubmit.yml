bcr_test_module:
  module_path: "e2e/smoke"
  matrix:
    platform:
      [
        "windows",
        "ubuntu2404",
        "macos",
        "macos_arm64",
        "ubuntu2404_arm64",
        "windows_arm64",
      ]
    bazel: ["8.x", "9.x"]
  tasks:
    run_tests:
      name: "Run smoke test"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
